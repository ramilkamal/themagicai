import React from 'react';
import { LoginModal } from './login-modal/loginModal';

export const Login: React.FC = () => {
  return (
    <div className="login">
      <LoginModal />
    </div>
  );
};
